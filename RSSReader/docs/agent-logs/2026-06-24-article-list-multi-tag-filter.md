# 2026-06-24 文章列表多标签筛选恢复

- 日期：2026-06-24
- 负责人：Codex
- 使用工具：Codex shell、apply_patch
- 对应 Issue / PR：暂无
- 任务目标：在保留左侧标签栏单标签快速入口的前提下，在文章列表顶部恢复多标签筛选能力，并支持任一 / 全部匹配切换。
- 关键 Prompt 摘要：用户确认按建议恢复多标签筛选，将复杂筛选从左侧标签栏迁移到中间文章列表头部。
- Agent 修改内容摘要：
  - 在 `App.tsx` 中新增文章列表专用的添加标签、移除标签、清空标签和切换匹配模式处理函数。
  - 在 `ArticleList.tsx` 中新增标签筛选条：已选标签 chip、添加标签弹层、标签搜索、任一 / 全部匹配切换。
  - 更新文章列表标题的标签匹配模式显示，避免直接展示 `any` / `all`。
  - 在 `theme-refresh.css` 和 `sidebar-refinements.css` 中补充筛选条、弹层、响应式和深色主题样式。
- 人工检查结果：前端构建通过；本地 dev server 可访问。
- 是否运行测试：
  - `npm run build`：沙箱内因 Vite/esbuild 子进程 `spawn EPERM` 失败；提权后通过，保留 Vite chunk size 警告。
  - `http://127.0.0.1:5178/`：返回 200。
- 未解决问题：
  - 未做浏览器截图级交互检查；后续可在真实数据中确认标签弹层位置和窄宽布局细节。
