# 2026-06-05 Translation Language and Selection Improvements

- 日期：2026-06-05
- 负责人：Codex
- 使用工具：Codex CLI、shell、apply_patch
- 对应 Issue / PR：未提供
- 任务目标：扩展翻译模块支持更多常见语言，并支持只翻译阅读器中选中的文字。
- 关键 Prompt 摘要：用户要求“语言选择框始终显示，翻译多种常见语言，不只有中英文；可以选中文字，仅针对选中文字进行翻译”。
- Agent 修改内容摘要：
  - 扩展前端阅读器翻译语言下拉，加入繁体中文、日语、韩语、法语、德语、西班牙语、意大利语、葡萄牙语、俄语、阿拉伯语、印地语、印尼语、越南语、泰语、土耳其语。
  - 在 `StartTranslationRequest` 中加入可选 `selectedText` 字段。
  - 后端翻译服务在收到 `selectedText` 时只翻译选区文本，返回单段翻译结果，不写入整篇文章翻译缓存。
  - 扩展后端语言代码到 Prompt 显示名的映射。
  - 调整双语视图文案和选区翻译结果样式。
- 人工检查结果：待人工在应用中检查语言下拉和选中文字翻译交互。
- 是否运行测试：
  - 已运行 `cargo check`，通过，存在既有 unused/dead_code 警告。
  - 已运行 `cargo test`，22 个测试通过，存在既有 unused/dead_code 警告。
  - 尝试运行 `npm run build`，失败于当前 Node.js v12.22.9 无法解析 TypeScript 编译器中的 `??` 语法，需要升级 Node 后复验。
- 未解决问题：
  - 前端构建需在 Node.js 版本满足项目工具链要求后重新运行。
  - 选区翻译结果当前不持久化到 SQLite，避免污染整篇文章翻译缓存；如需选区翻译历史，需要单独设计存储表或缓存策略。
