# 2026-06-24 标签侧栏简化重构

- 日期：2026-06-24
- 负责人：Codex
- 使用工具：Codex shell、apply_patch
- 对应 Issue / PR：暂无
- 任务目标：按照讨论方案重构左侧标签展示，让默认标签区更像快速筛选入口，降低多标签匹配和管理操作带来的复杂度。
- 关键 Prompt 摘要：用户确认按建议修改标签展示；建议方向为左侧只保留搜索和单标签筛选，复杂管理动作放到管理入口。
- Agent 修改内容摘要：
  - 将 `TagWorkspace` 拆成默认快速筛选视图和隐藏的管理标签视图。
  - 默认视图只展示搜索框、按使用次数排序的标签列表，以及清除当前标签筛选按钮。
  - 管理视图通过工具按钮进入，保留重命名、合并、删除标签能力。
  - 将标签筛选交互改成单标签选择；再次点击当前标签或清除按钮回到全部文章。
  - 移除未再使用的多标签 AND/OR、排序下拉和已选标签条样式。
- 人工检查结果：前端构建通过，本地 dev server 可访问。
- 是否运行测试：
  - `npm run build`：通过，保留 Vite chunk size 警告。
  - `npm run dev -- --host 127.0.0.1 --port 5177`：5177 被占用后 Vite 自动使用 5178，`http://127.0.0.1:5178/` 返回 200。
- 未解决问题：
  - 后端标签数据结构仍保留多标签筛选能力，便于以后在文章列表顶部筛选条中重新设计多标签筛选。
